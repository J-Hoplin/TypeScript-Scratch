/**
 * 어노테이션을 사용하면 타입스크립트에 명시적으로 타입을 지정할 수 있다
 * 
 * value: type 형태로 작성해준다 다만, 타입스크립트가 타입을 추론하도록 두는것이 코드를 줄이는 것이므로, 어노테이션은 생략한다.
 */

let a: number = 1;
let b: string = 'hello';
let c: boolean[] = [true, false]